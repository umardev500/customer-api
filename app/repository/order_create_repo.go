package repository

import (
	"context"
	"customer-api/pb"
)

func (o *orderRepository) CreateOrder(ctx context.Context, req *pb.OrderCreateRequest) (err error) {
	// _, err = o.order.Create(ctx, req)

	return
}
